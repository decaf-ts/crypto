/**
 * @description Defines keys for cryptographic metadata.
 * @summary
 * This enum provides a set of keys used for storing and retrieving cryptographic metadata
 * within the decoration system.
 * @enum {string}
 * @readonly
 * @const CryptoKeys
 * @memberOf module:@decaf-ts/crypto
 */
export enum CryptoKeys {
  /**
   * @description Key for storing encryption metadata.
   */
  ENCRYPTED = "encrypted",
}
